class Color {
    constructor(r, g, b, speed) {
        this.r = r
        this.g = g
        this.b = b
        this.speed = speed
    }

    setSpeed(newSpeed) {
        this.speed = newSpeed
    }
}